# Responsive pharmacy dashboard shell plan

1. Add the branded icon to the runtime asset manifest and add localized shell
   labels.
2. Add a desktop sidebar with permission-aware grouped destinations, breadcrumb,
   and semantic keyboard-focusable controls.
3. Add mobile bottom navigation for dashboard, POS, inventory, and alerts.
4. Add wide/phone widget tests and fix responsive overflow assertions.
5. Run generation, analyzer, coverage, Windows build, PR CI, and post-merge
   main CI. This is a visual PR and remains available for user review before
   merge.
