# Accessibility foundation plan

1. Add a regression test for desktop sidebar navigation semantics.
2. Add localized tooltips to inventory refresh and POS add-to-cart actions.
3. Regenerate localization output and run analyzer, full tests, and a Windows build.
4. Create a PR, wait for all CI checks, merge, and verify main.
