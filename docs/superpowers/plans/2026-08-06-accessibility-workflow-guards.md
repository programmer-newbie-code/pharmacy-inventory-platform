# Accessibility workflow guards plan

1. Add shell widget coverage for semantic labels, 48dp targets, focus, keyboard
   activation, and phone navigation.
2. Run the focused test, analyzer, full coverage suite, and Windows build.
3. Submit a signed PR; require full CI and green main before the next increment.
